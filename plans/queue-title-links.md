# Plan: Queue title links

Queue titles open `/movies/:id` or `/series/episodes/:id`, the same pages as Movies, Series, Suggestions, and Errors. `GET /api/jobs` includes `href` when the library item still exists.
