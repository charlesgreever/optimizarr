# Plan: Optional subtitle language ID

Identify language on untagged **text** subtitle tracks (SRT/ASS/WebVTT). Extract a few minutes as SubRip, detect from the words, confirm before saving on the inspection. PGS/VobSub image subs are out of scope (no OCR). Library file unchanged until Keep. No Whisper and no extra env var.
