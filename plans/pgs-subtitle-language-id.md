# Plan: Identify language on untagged PGS subtitles

Optional title-page **Identify language** for untagged PGS. ffmpeg copies a 180-second `.sup` sample; `PGS_OCR` OCRs at most 24 images with Tesseract (OSD+Latin); Node `franc` maps the text. Confirm before save. Inspection only until Keep. VobSub/DVB and full-track SRT conversion are out of scope.
