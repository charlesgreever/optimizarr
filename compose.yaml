name: optimizarr
services:
  optimizarr:
    build: .
    image: optimizarr:local
    container_name: optimizarr
    restart: unless-stopped
    environment:
      PUID: "1000"
      PGID: "1000"
      TZ: America/New_York
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,video,utility
    ports:
      - "7373:7373"
    volumes:
      # Same layout as the other *arrs on ubuntuserver
      - /home/cgreever/appdata/arr/optimizarr/config:/config
      - /mnt/nas:/mnt/nas
      - /mnt/nas/sabdownloads/complete:/complete
      - /mnt/nas/sabdownloads/incomplete:/incomplete
      # Optional NAS-native copies: put a Synology SSH key at
      # /home/cgreever/appdata/arr/optimizarr/config/nas_id_ed25519
      # then set host/user/maps in Settings. The config volume already exposes it as /config/nas_id_ed25519.
    networks:
      - arr_net
    runtime: nvidia
    labels:
      icon: https://cdn.jsdelivr.net/gh/walkxcode/dashboard-icons/png/radarr.png

networks:
  arr_net:
    external: true
